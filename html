<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>OnRoad Motors— Luxury Automotive Bahrain</title>
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,600;1,300;1,400&family=Montserrat:wght@300;400;500;600;700&family=Playfair+Display:ital,wght@0,700;1,600&display=swap" rel="stylesheet">
<style>
  :root {
    --gold: #C9A84C;
    --gold-light: #E8C97A;
    --gold-pale: #F5E6C0;
    --black: #080808;
    --black-rich: #0D0D0D;
    --black-card: #111111;
    --black-border: #1E1E1E;
    --white: #F8F5EF;
    --white-dim: rgba(248,245,239,0.7);
    --font-display: 'Playfair Display', serif;
    --font-heading: 'Cormorant Garamond', serif;
    --font-body: 'Montserrat', sans-serif;
  }

  *, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }

  html { scroll-behavior: smooth; }

  body {
    background: var(--black);
    color: var(--white);
    font-family: var(--font-body);
    overflow-x: hidden;
    cursor: none;
  }

  /* ── CUSTOM CURSOR ── */
  #cursor {
    position: fixed; top: 0; left: 0; z-index: 9999;
    pointer-events: none;
    mix-blend-mode: difference;
  }
  #cursor-dot {
    width: 8px; height: 8px;
    background: var(--gold);
    border-radius: 50%;
    position: absolute; transform: translate(-50%,-50%);
    transition: transform .1s ease;
  }
  #cursor-ring {
    width: 36px; height: 36px;
    border: 1px solid var(--gold-light);
    border-radius: 50%;
    position: absolute; transform: translate(-50%,-50%);
    transition: transform .18s ease, width .3s, height .3s, border-color .3s;
  }
  body:hover #cursor-ring { opacity: 1; }
  a:hover ~ #cursor #cursor-ring,
  button:hover ~ #cursor #cursor-ring { width: 52px; height: 52px; border-color: var(--gold); }

  /* ── SCROLLBAR ── */
  ::-webkit-scrollbar { width: 4px; }
  ::-webkit-scrollbar-track { background: var(--black); }
  ::-webkit-scrollbar-thumb { background: var(--gold); border-radius: 2px; }

  /* ── NAV ── */
  nav {
    position: fixed; top: 0; left: 0; right: 0; z-index: 1000;
    display: flex; align-items: center; justify-content: space-between;
    padding: 1.4rem 5vw;
    transition: background .4s, backdrop-filter .4s, padding .4s;
  }
  nav.scrolled {
    background: rgba(8,8,8,0.92);
    backdrop-filter: blur(18px);
    padding: 1rem 5vw;
    border-bottom: 1px solid var(--black-border);
  }
  .nav-logo {
    font-family: var(--font-heading);
    font-size: 1.5rem; font-weight: 600; letter-spacing: .12em;
    color: var(--gold); text-decoration: none;
    display: flex; flex-direction: column; line-height: 1;
  }
  .nav-logo span { font-size: .6rem; letter-spacing: .4em; color: var(--white-dim); font-family: var(--font-body); font-weight: 300; margin-top: 3px; }
  .nav-links { display: flex; gap: 2.5rem; list-style: none; }
  .nav-links a {
    font-size: .72rem; letter-spacing: .2em; text-transform: uppercase;
    color: var(--white-dim); text-decoration: none;
    transition: color .3s; position: relative;
  }
  .nav-links a::after {
    content: ''; position: absolute; bottom: -3px; left: 0;
    width: 0; height: 1px; background: var(--gold);
    transition: width .3s;
  }
  .nav-links a:hover { color: var(--gold); }
  .nav-links a:hover::after { width: 100%; }
  .nav-cta {
    font-family: var(--font-body); font-size: .7rem; letter-spacing: .2em; text-transform: uppercase;
    padding: .6rem 1.6rem; border: 1px solid var(--gold);
    color: var(--gold); text-decoration: none; background: transparent;
    transition: background .3s, color .3s;
  }
  .nav-cta:hover { background: var(--gold); color: var(--black); }
  .hamburger { display: none; flex-direction: column; gap: 5px; cursor: none; border: none; background: none; }
  .hamburger span { width: 24px; height: 1px; background: var(--white); transition: .3s; }

  /* ── HERO ── */
  #hero {
    position: relative; min-height: 100vh;
    display: flex; flex-direction: column; justify-content: center;
    overflow: hidden; padding: 0 5vw;
  }
  .hero-bg {
    position: absolute; inset: 0; z-index: 0;
    background:
      radial-gradient(ellipse 80% 60% at 70% 60%, rgba(201,168,76,.07) 0%, transparent 70%),
      radial-gradient(ellipse 40% 40% at 20% 80%, rgba(201,168,76,.04) 0%, transparent 60%),
      linear-gradient(160deg, #0a0a0a 0%, #050505 100%);
  }
  .hero-grid-lines {
    position: absolute; inset: 0; z-index: 0;
    background-image:
      linear-gradient(rgba(201,168,76,.04) 1px, transparent 1px),
      linear-gradient(90deg, rgba(201,168,76,.04) 1px, transparent 1px);
    background-size: 80px 80px;
    mask-image: radial-gradient(ellipse 90% 90% at 50% 50%, black 30%, transparent 80%);
  }
  .hero-content { position: relative; z-index: 2; max-width: 700px; }
  .hero-eyebrow {
    font-size: .65rem; letter-spacing: .5em; text-transform: uppercase;
    color: var(--gold); margin-bottom: 1.5rem;
    opacity: 0; transform: translateY(20px);
    animation: fadeUp .8s .3s forwards;
    display: flex; align-items: center; gap: 1rem;
  }
  .hero-eyebrow::before { content: ''; width: 40px; height: 1px; background: var(--gold); }
  .hero-title {
    font-family: var(--font-display);
    font-size: clamp(3rem, 7vw, 6.5rem);
    font-weight: 700; line-height: 1.05;
    letter-spacing: -.01em;
    opacity: 0; transform: translateY(30px);
    animation: fadeUp .9s .5s forwards;
  }
  .hero-title em { font-style: italic; color: var(--gold); }
  .hero-subtitle {
    margin-top: 1.5rem;
    font-size: .85rem; letter-spacing: .1em; line-height: 1.9;
    color: var(--white-dim); max-width: 480px;
    opacity: 0; transform: translateY(20px);
    animation: fadeUp .8s .8s forwards;
  }
  .hero-actions {
    margin-top: 2.5rem; display: flex; gap: 1.2rem; flex-wrap: wrap;
    opacity: 0; transform: translateY(20px);
    animation: fadeUp .8s 1s forwards;
  }
  .btn-gold {
    padding: .85rem 2.2rem; background: var(--gold);
    color: var(--black); font-size: .72rem; letter-spacing: .2em; text-transform: uppercase;
    font-weight: 600; text-decoration: none; border: none; cursor: none;
    transition: background .3s, transform .2s;
    font-family: var(--font-body);
    clip-path: polygon(0 0, calc(100% - 10px) 0, 100% 10px, 100% 100%, 10px 100%, 0 calc(100% - 10px));
  }
  .btn-gold:hover { background: var(--gold-light); transform: translateY(-2px); }
  .btn-outline {
    padding: .85rem 2.2rem; background: transparent;
    border: 1px solid rgba(201,168,76,.4); color: var(--gold);
    font-size: .72rem; letter-spacing: .2em; text-transform: uppercase;
    font-weight: 500; text-decoration: none; cursor: none;
    transition: border-color .3s, transform .2s;
    font-family: var(--font-body);
  }
  .btn-outline:hover { border-color: var(--gold); transform: translateY(-2px); }

  /* Car SVG Silhouette */
  .hero-car {
    position: absolute; right: -2%; bottom: 8%;
    width: min(55vw, 700px); z-index: 1;
    opacity: 0; transform: translateX(80px);
    animation: carSlide 1.2s .6s cubic-bezier(.22,1,.36,1) forwards;
  }
  .hero-car svg { width: 100%; filter: drop-shadow(0 0 40px rgba(201,168,76,.15)); }

  .hero-scroll {
    position: absolute; bottom: 3rem; left: 50%; transform: translateX(-50%);
    z-index: 2; display: flex; flex-direction: column; align-items: center; gap: .5rem;
    opacity: 0; animation: fadeUp .6s 1.4s forwards;
  }
  .hero-scroll span { font-size: .6rem; letter-spacing: .3em; text-transform: uppercase; color: var(--white-dim); }
  .scroll-line { width: 1px; height: 50px; background: linear-gradient(var(--gold), transparent); animation: scrollPulse 1.8s infinite; }

  /* ── STATS BAR ── */
  #stats {
    background: var(--black-card);
    border-top: 1px solid var(--black-border);
    border-bottom: 1px solid var(--black-border);
    padding: 3rem 5vw;
  }
  .stats-grid {
    display: grid; grid-template-columns: repeat(4,1fr);
    gap: 2rem; max-width: 1200px; margin: 0 auto;
  }
  .stat-item { text-align: center; }
  .stat-number {
    font-family: var(--font-heading);
    font-size: clamp(2.2rem, 4vw, 3.5rem);
    font-weight: 600; color: var(--gold);
    letter-spacing: -.02em; line-height: 1;
  }
  .stat-label {
    font-size: .65rem; letter-spacing: .3em; text-transform: uppercase;
    color: var(--white-dim); margin-top: .6rem;
  }
  .stat-divider { width: 24px; height: 1px; background: var(--gold); margin: .7rem auto 0; }

  /* ── SECTION SHARED ── */
  section { padding: 7rem 5vw; }
  .section-label {
    font-size: .6rem; letter-spacing: .5em; text-transform: uppercase;
    color: var(--gold); display: flex; align-items: center; gap: 1rem;
    margin-bottom: 1rem;
  }
  .section-label::before { content: ''; width: 30px; height: 1px; background: var(--gold); }
  .section-title {
    font-family: var(--font-display);
    font-size: clamp(2rem, 4vw, 3.2rem);
    line-height: 1.15; font-weight: 700;
    max-width: 600px;
  }
  .section-title em { font-style: italic; color: var(--gold); }
  .section-subtitle {
    font-size: .82rem; letter-spacing: .05em; line-height: 1.9;
    color: var(--white-dim); max-width: 520px; margin-top: 1.2rem;
  }

  /* ── INVENTORY ── */
  #inventory { background: var(--black); }
  .inventory-header { text-align: center; margin-bottom: 4rem; }
  .inventory-header .section-label { justify-content: center; }
  .inventory-header .section-title { margin: 0 auto; text-align: center; }
  .inventory-header .section-subtitle { margin: 1.2rem auto 0; text-align: center; }

  .cars-grid {
    display: grid; grid-template-columns: repeat(3, 1fr);
    gap: 1.5rem; max-width: 1280px; margin: 0 auto;
  }
  .car-card {
    background: var(--black-card);
    border: 1px solid var(--black-border);
    overflow: hidden; position: relative;
    transition: border-color .4s, transform .4s;
  }
  .car-card:hover { border-color: rgba(201,168,76,.4); transform: translateY(-6px); }
  .car-card-badge {
    position: absolute; top: 1.2rem; left: 1.2rem; z-index: 2;
    font-size: .58rem; letter-spacing: .25em; text-transform: uppercase;
    padding: .3rem .8rem; background: var(--gold); color: var(--black); font-weight: 700;
  }
  .car-image-wrap {
    height: 220px; overflow: hidden; position: relative;
    background: linear-gradient(135deg, #0f0f0f 0%, #1a1a1a 100%);
    display: flex; align-items: center; justify-content: center;
  }
  .car-image-wrap::after {
    content: ''; position: absolute; bottom: 0; left: 0; right: 0;
    height: 60px;
    background: linear-gradient(transparent, var(--black-card));
  }
  .car-photo { width: 100%; height: 100%; object-fit: cover; object-position: center; transition: transform .5s; }
  .car-card:hover .car-photo { transform: scale(1.06); }
  .car-info { padding: 1.6rem; }
  .car-brand { font-size: .6rem; letter-spacing: .35em; text-transform: uppercase; color: var(--gold); }
  .car-name {
    font-family: var(--font-heading); font-size: 1.6rem; font-weight: 600;
    margin-top: .3rem; letter-spacing: .02em;
  }
  .car-specs {
    display: flex; gap: 1.2rem; margin-top: 1rem;
    padding-top: 1rem; border-top: 1px solid var(--black-border);
  }
  .car-spec { flex: 1; }
  .car-spec-val { font-size: .85rem; font-weight: 600; color: var(--white); }
  .car-spec-key { font-size: .58rem; letter-spacing: .2em; text-transform: uppercase; color: var(--white-dim); margin-top: .15rem; }
  .car-price-row { display: flex; align-items: center; justify-content: space-between; margin-top: 1.2rem; }
  .car-price {
    font-family: var(--font-heading); font-size: 1.5rem; font-weight: 600; color: var(--gold);
  }
  .car-price span { font-size: .65rem; font-family: var(--font-body); color: var(--white-dim); letter-spacing: .1em; }
  .car-btn {
    font-size: .62rem; letter-spacing: .2em; text-transform: uppercase;
    padding: .5rem 1.2rem; border: 1px solid var(--gold);
    color: var(--gold); background: transparent; cursor: none;
    transition: background .3s, color .3s; font-family: var(--font-body);
  }
  .car-btn:hover { background: var(--gold); color: var(--black); }

  /* ── SERVICES ── */
  #services { background: var(--black-card); }
  .services-layout { display: grid; grid-template-columns: 1fr 2fr; gap: 4rem; max-width: 1280px; margin: 0 auto; align-items: start; }
  .services-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 1.2rem; }
  .service-card {
    padding: 1.8rem 1.4rem;
    border: 1px solid var(--black-border);
    background: var(--black);
    transition: border-color .4s, background .3s;
    cursor: default;
  }
  .service-card:hover { border-color: rgba(201,168,76,.35); background: rgba(201,168,76,.03); }
  .service-icon {
    width: 42px; height: 42px; margin-bottom: 1.2rem;
    background: rgba(201,168,76,.08); border: 1px solid rgba(201,168,76,.2);
    display: flex; align-items: center; justify-content: center;
    font-size: 1.2rem;
  }
  .service-name {
    font-family: var(--font-heading); font-size: 1.1rem; font-weight: 600;
    letter-spacing: .04em; margin-bottom: .6rem;
  }
  .service-desc { font-size: .72rem; line-height: 1.8; color: var(--white-dim); letter-spacing: .03em; }

  /* ── WHY US ── */
  #why { background: var(--black); }
  .why-layout { display: grid; grid-template-columns: 1fr 1fr; gap: 5rem; max-width: 1280px; margin: 0 auto; align-items: center; }
  .why-visual {
    position: relative; height: 500px;
    display: flex; align-items: center; justify-content: center;
  }
  .why-ring {
    position: absolute;
    border: 1px solid rgba(201,168,76,.15);
    border-radius: 50%;
    animation: spinSlow 20s linear infinite;
  }
  .why-ring:nth-child(1) { width: 400px; height: 400px; }
  .why-ring:nth-child(2) { width: 300px; height: 300px; animation-duration: 14s; animation-direction: reverse; border-color: rgba(201,168,76,.1); }
  .why-ring:nth-child(3) { width: 180px; height: 180px; animation-duration: 8s; }
  .why-center {
    position: relative; z-index: 2;
    width: 120px; height: 120px;
    background: var(--black-card);
    border: 1px solid rgba(201,168,76,.3);
    border-radius: 50%;
    display: flex; flex-direction: column; align-items: center; justify-content: center;
    gap: .2rem;
  }
  .why-center-num { font-family: var(--font-heading); font-size: 2rem; font-weight: 600; color: var(--gold); line-height: 1; }
  .why-center-label { font-size: .55rem; letter-spacing: .25em; text-transform: uppercase; color: var(--white-dim); }
  .why-points { display: flex; flex-direction: column; gap: 1.4rem; margin-top: 2.5rem; }
  .why-point { display: flex; gap: 1.2rem; align-items: flex-start; }
  .why-point-num {
    font-family: var(--font-heading); font-size: 1.8rem; font-weight: 300;
    color: rgba(201,168,76,.3); line-height: 1; min-width: 2rem;
  }
  .why-point-title { font-size: .8rem; font-weight: 600; letter-spacing: .08em; margin-bottom: .3rem; }
  .why-point-text { font-size: .72rem; line-height: 1.8; color: var(--white-dim); }

  /* ── BOOKING ── */
  #booking {
    background: linear-gradient(160deg, #0a0a0a 0%, var(--black-card) 100%);
    border-top: 1px solid var(--black-border);
  }
  .booking-layout { display: grid; grid-template-columns: 1fr 1fr; gap: 5rem; max-width: 1280px; margin: 0 auto; }
  .booking-info-list { margin-top: 2.5rem; display: flex; flex-direction: column; gap: 1rem; }
  .booking-info-item { display: flex; gap: 1rem; align-items: flex-start; }
  .booking-info-icon {
    width: 36px; height: 36px; border: 1px solid rgba(201,168,76,.3);
    display: flex; align-items: center; justify-content: center;
    font-size: .9rem; flex-shrink: 0;
  }
  .booking-info-text { font-size: .78rem; line-height: 1.7; }
  .booking-info-text strong { display: block; font-size: .68rem; letter-spacing: .2em; text-transform: uppercase; color: var(--gold); margin-bottom: .2rem; }
  .booking-info-text span { color: var(--white-dim); }

  .booking-form { display: flex; flex-direction: column; gap: 1.2rem; }
  .form-row { display: grid; grid-template-columns: 1fr 1fr; gap: 1.2rem; }
  .form-group { display: flex; flex-direction: column; gap: .5rem; }
  .form-group label { font-size: .62rem; letter-spacing: .25em; text-transform: uppercase; color: var(--gold); }
  .form-group input,
  .form-group select,
  .form-group textarea {
    background: var(--black); border: 1px solid var(--black-border);
    color: var(--white); padding: .85rem 1rem;
    font-family: var(--font-body); font-size: .78rem;
    outline: none; transition: border-color .3s;
    appearance: none;
  }
  .form-group input:focus,
  .form-group select:focus,
  .form-group textarea:focus { border-color: var(--gold); }
  .form-group textarea { resize: vertical; min-height: 100px; }
  .form-submit {
    padding: 1rem 2.5rem; background: var(--gold);
    color: var(--black); font-size: .72rem; letter-spacing: .25em;
    text-transform: uppercase; font-weight: 700; border: none; cursor: none;
    font-family: var(--font-body); transition: background .3s, transform .2s;
    clip-path: polygon(0 0, calc(100% - 12px) 0, 100% 12px, 100% 100%, 12px 100%, 0 calc(100% - 12px));
    align-self: flex-start;
  }
  .form-submit:hover { background: var(--gold-light); transform: translateY(-2px); }

  /* ── FOOTER ── */
  footer {
    background: #050505;
    border-top: 1px solid var(--black-border);
    padding: 5rem 5vw 2rem;
  }
  .footer-top {
    display: grid; grid-template-columns: 2fr 1fr 1fr 1fr;
    gap: 3rem; max-width: 1280px; margin: 0 auto;
    padding-bottom: 3rem; border-bottom: 1px solid var(--black-border);
  }
  .footer-brand-name {
    font-family: var(--font-heading); font-size: 1.6rem; font-weight: 600;
    color: var(--gold); letter-spacing: .1em;
  }
  .footer-brand-tag { font-size: .6rem; letter-spacing: .4em; text-transform: uppercase; color: var(--white-dim); margin-top: .4rem; }
  .footer-brand-desc { font-size: .75rem; line-height: 1.9; color: var(--white-dim); margin-top: 1.2rem; max-width: 280px; }
  .footer-socials { display: flex; gap: .8rem; margin-top: 1.5rem; }
  .footer-social {
    width: 36px; height: 36px; border: 1px solid var(--black-border);
    display: flex; align-items: center; justify-content: center;
    font-size: .75rem; color: var(--white-dim); text-decoration: none;
    transition: border-color .3s, color .3s;
  }
  .footer-social:hover { border-color: var(--gold); color: var(--gold); }
  .footer-col-title { font-size: .62rem; letter-spacing: .35em; text-transform: uppercase; color: var(--gold); margin-bottom: 1.4rem; }
  .footer-links { list-style: none; display: flex; flex-direction: column; gap: .7rem; }
  .footer-links a { font-size: .75rem; color: var(--white-dim); text-decoration: none; letter-spacing: .04em; transition: color .3s; }
  .footer-links a:hover { color: var(--gold); }
  .footer-bottom {
    max-width: 1280px; margin: 2rem auto 0;
    display: flex; justify-content: space-between; align-items: center;
    font-size: .65rem; letter-spacing: .1em; color: rgba(248,245,239,.35);
  }
  .footer-bottom a { color: var(--gold); text-decoration: none; }

  /* ── REVEAL ANIMATIONS ── */
  .reveal { opacity: 0; transform: translateY(40px); transition: opacity .8s ease, transform .8s ease; }
  .reveal.visible { opacity: 1; transform: none; }
  .reveal-left { opacity: 0; transform: translateX(-40px); transition: opacity .8s ease, transform .8s ease; }
  .reveal-left.visible { opacity: 1; transform: none; }
  .reveal-right { opacity: 0; transform: translateX(40px); transition: opacity .8s ease, transform .8s ease; }
  .reveal-right.visible { opacity: 1; transform: none; }

  /* ── KEYFRAMES ── */
  @keyframes fadeUp { to { opacity: 1; transform: none; } }
  @keyframes carSlide { to { opacity: 1; transform: none; } }
  @keyframes scrollPulse { 0%,100%{ opacity: .3; transform: scaleY(.5); } 50%{ opacity: 1; transform: scaleY(1); } }
  @keyframes spinSlow { to { transform: rotate(360deg); } }

  /* ── GOLD DIVIDER ── */
  .gold-line { width: 60px; height: 2px; background: linear-gradient(90deg, var(--gold), var(--gold-light)); margin: 1.5rem 0; }

  /* ── RESPONSIVE ── */
  @media (max-width: 1024px) {
    .cars-grid { grid-template-columns: repeat(2,1fr); }
    .services-layout { grid-template-columns: 1fr; }
    .why-layout { grid-template-columns: 1fr; }
    .booking-layout { grid-template-columns: 1fr; }
    .footer-top { grid-template-columns: 1fr 1fr; }
    .hero-car { opacity: .3; }
  }
  @media (max-width: 768px) {
    .cars-grid { grid-template-columns: 1fr; }
    .stats-grid { grid-template-columns: repeat(2,1fr); }
    .services-grid { grid-template-columns: repeat(2,1fr); }
    .form-row { grid-template-columns: 1fr; }
    .footer-top { grid-template-columns: 1fr; }
    .footer-bottom { flex-direction: column; gap: .5rem; text-align: center; }
    .nav-links, .nav-cta { display: none; }
    .hamburger { display: flex; }
    .hero-title { font-size: clamp(2.4rem, 9vw, 4rem); }
    .hero-car { display: none; }
    .why-visual { height: 300px; }
    .why-ring:nth-child(1) { width: 260px; height: 260px; }
    .why-ring:nth-child(2) { width: 190px; height: 190px; }
    .why-ring:nth-child(3) { width: 110px; height: 110px; }
  }
  @media (max-width: 480px) {
    .services-grid { grid-template-columns: 1fr; }
  }

  /* Mobile menu */
  .mobile-menu {
    display: none; position: fixed; inset: 0; background: rgba(5,5,5,.97); z-index: 999;
    flex-direction: column; align-items: center; justify-content: center; gap: 2.5rem;
  }
  .mobile-menu.open { display: flex; }
  .mobile-menu a {
    font-family: var(--font-heading); font-size: 2rem; font-weight: 600;
    color: var(--white); text-decoration: none; letter-spacing: .05em;
    transition: color .3s;
  }
  .mobile-menu a:hover { color: var(--gold); }
  .mobile-menu-close {
    position: absolute; top: 1.5rem; right: 5vw;
    background: none; border: none; color: var(--white-dim); font-size: 1.4rem; cursor: none;
  }
</style>
</head>
<body>

<!-- CURSOR -->
<div id="cursor"><div id="cursor-dot"></div><div id="cursor-ring"></div></div>

<!-- MOBILE MENU -->
<div class="mobile-menu" id="mobileMenu">
  <button class="mobile-menu-close" id="menuClose">✕</button>
  <a href="#inventory">Inventory</a>
  <a href="#services">Services</a>
  <a href="#why">About</a>
  <a href="#booking">Contact</a>
</div>

<!-- NAV -->
<nav id="navbar">
  <a href="#hero" class="nav-logo">NEW CAR SHOWROOM<span>Luxury Automotive · Bahrain</span></a>
  <ul class="nav-links">
    <li><a href="#inventory">Inventory</a></li>
    <li><a href="#services">Services</a></li>
    <li><a href="#why">About</a></li>
    <li><a href="#booking">Contact</a></li>
  </ul>
  <a href="#booking" class="nav-cta">Book a Visit</a>
  <button class="hamburger" id="hamburger" aria-label="Menu">
    <span></span><span></span><span></span>
  </button>
</nav>

<!-- HERO -->
<section id="hero">
  <div class="hero-bg"></div>
  <div class="hero-grid-lines"></div>

  <div class="hero-content">
    <div class="hero-eyebrow">Bahrain's Premier Luxury Dealer</div>
    <h1 class="hero-title">Drive the <em>Extraordinary</em></h1>
    <p class="hero-subtitle">Curated luxury automobiles. Bespoke service. Uncompromising standards. Welcome to New Car Showroom — where passion for the exceptional meets the road.</p>
    <div class="hero-actions">
      <a href="#inventory" class="btn-gold">Explore Inventory</a>
      <a href="#booking" class="btn-outline">Book a Test Drive</a>
    </div>
  </div>

  <!-- Luxury Car SVG Silhouette -->
  <div class="hero-car">
    <svg viewBox="0 0 900 320" xmlns="http://www.w3.org/2000/svg" fill="none">
      <!-- Road -->
      <ellipse cx="450" cy="295" rx="420" ry="18" fill="rgba(201,168,76,0.04)"/>
      <!-- Body shadow -->
      <ellipse cx="450" cy="285" rx="340" ry="12" fill="rgba(0,0,0,0.5)"/>
      <!-- Main body -->
      <path d="M 80 230 Q 80 200 120 190 L 220 150 Q 290 110 370 100 L 530 100 Q 620 100 680 140 L 800 195 Q 830 205 830 230 L 830 255 Q 830 270 810 270 L 90 270 Q 75 270 80 255 Z" fill="#1a1a1a" stroke="rgba(201,168,76,0.3)" stroke-width="1.5"/>
      <!-- Roof -->
      <path d="M 240 195 Q 260 140 320 120 L 490 110 Q 570 108 620 140 L 700 195" fill="#141414" stroke="rgba(201,168,76,0.2)" stroke-width="1"/>
      <!-- Windows -->
      <path d="M 258 190 Q 275 148 325 130 L 430 120 L 430 188 Z" fill="rgba(201,168,76,0.06)" stroke="rgba(201,168,76,0.25)" stroke-width="1"/>
      <path d="M 440 118 L 565 118 Q 615 120 650 155 L 660 190 L 440 190 Z" fill="rgba(201,168,76,0.06)" stroke="rgba(201,168,76,0.25)" stroke-width="1"/>
      <!-- Door lines -->
      <line x1="435" y1="115" x2="435" y2="265" stroke="rgba(201,168,76,0.15)" stroke-width="1"/>
      <line x1="620" y1="145" x2="670" y2="265" stroke="rgba(201,168,76,0.12)" stroke-width="1"/>
      <!-- Headlight -->
      <ellipse cx="805" cy="225" rx="22" ry="12" fill="rgba(201,168,76,0.15)" stroke="rgba(201,168,76,0.6)" stroke-width="1.5"/>
      <ellipse cx="805" cy="225" rx="12" ry="6" fill="rgba(201,168,76,0.3)"/>
      <!-- Taillight -->
      <rect x="92" y="218" width="18" height="22" rx="3" fill="rgba(201,168,76,0.08)" stroke="rgba(201,168,76,0.4)" stroke-width="1.5"/>
      <!-- Front wheels -->
      <circle cx="670" cy="272" r="45" fill="#0d0d0d" stroke="rgba(201,168,76,0.4)" stroke-width="2"/>
      <circle cx="670" cy="272" r="30" fill="#111" stroke="rgba(201,168,76,0.2)" stroke-width="1"/>
      <circle cx="670" cy="272" r="14" fill="#0a0a0a" stroke="rgba(201,168,76,0.5)" stroke-width="1.5"/>
      <circle cx="670" cy="272" r="4" fill="rgba(201,168,76,0.6)"/>
      <!-- Rear wheels -->
      <circle cx="225" cy="272" r="45" fill="#0d0d0d" stroke="rgba(201,168,76,0.4)" stroke-width="2"/>
      <circle cx="225" cy="272" r="30" fill="#111" stroke="rgba(201,168,76,0.2)" stroke-width="1"/>
      <circle cx="225" cy="272" r="14" fill="#0a0a0a" stroke="rgba(201,168,76,0.5)" stroke-width="1.5"/>
      <circle cx="225" cy="272" r="4" fill="rgba(201,168,76,0.6)"/>
      <!-- Chrome trim -->
      <path d="M 140 250 L 580 250" stroke="rgba(201,168,76,0.2)" stroke-width="1"/>
      <!-- Grill -->
      <rect x="795" y="235" width="20" height="18" rx="2" fill="rgba(201,168,76,0.05)" stroke="rgba(201,168,76,0.25)" stroke-width="1"/>
      <!-- Glow effect -->
      <ellipse cx="805" cy="225" rx="50" ry="25" fill="rgba(201,168,76,0.02)"/>
    </svg>
  </div>

  <div class="hero-scroll">
    <div class="scroll-line"></div>
    <span>Scroll</span>
  </div>
</section>

<!-- STATS -->
<div id="stats">
  <div class="stats-grid">
    <div class="stat-item reveal">
      <div class="stat-number" data-target="1400">0</div>
      <div class="stat-divider"></div>
      <div class="stat-label">Vehicles Sold</div>
    </div>
    <div class="stat-item reveal" style="transition-delay:.1s">
      <div class="stat-number" data-target="18">0</div>
      <div class="stat-divider"></div>
      <div class="stat-label">Luxury Brands</div>
    </div>
    <div class="stat-item reveal" style="transition-delay:.2s">
      <div class="stat-number" data-target="15">0</div>
      <div class="stat-divider"></div>
      <div class="stat-label">Years of Excellence</div>
    </div>
    <div class="stat-item reveal" style="transition-delay:.3s">
      <div class="stat-number" data-target="98">0</div>
      <div class="stat-divider"></div>
      <div class="stat-label">% Client Satisfaction</div>
    </div>
  </div>
</div>

<!-- INVENTORY -->
<section id="inventory">
  <div class="inventory-header">
    <div class="section-label">Featured Collection</div>
    <h2 class="section-title">Masterpieces of <em>Automotive</em> Art</h2>
    <p class="section-subtitle">A hand-selected lineup of the world's most coveted automobiles, ready for delivery in Bahrain.</p>
  </div>

  <div class="cars-grid">
    <!-- Mercedes -->
    <div class="car-card reveal">
      <div class="car-card-badge">New Arrival</div>
      <div class="car-image-wrap">
        <img class="car-photo" src="https://images.unsplash.com/photo-1618843479313-40f8afb4b4d8?w=900&q=85&auto=format&fit=crop" alt="Mercedes-Benz S-Class 500">
      </div>
      <div class="car-info">
        <div class="car-brand">Mercedes-Benz</div>
        <div class="car-name">S-Class 500</div>
        <div class="car-specs">
          <div class="car-spec"><div class="car-spec-val">4.0L</div><div class="car-spec-key">Engine</div></div>
          <div class="car-spec"><div class="car-spec-val">496 hp</div><div class="car-spec-key">Power</div></div>
          <div class="car-spec"><div class="car-spec-val">4.3s</div><div class="car-spec-key">0–100</div></div>
        </div>
        <div class="car-price-row">
          <div class="car-price">BD 42,000 <span>starting</span></div>
          <button class="car-btn" onclick="document.getElementById('booking').scrollIntoView({behavior:'smooth'})">Inquire</button>
        </div>
      </div>
    </div>

    <!-- BMW -->
    <div class="car-card reveal" style="transition-delay:.15s">
      <div class="car-card-badge" style="background:transparent;border:1px solid var(--gold);color:var(--gold)">In Stock</div>
      <div class="car-image-wrap">
        <img class="car-photo" src="https://images.unsplash.com/photo-1580273916550-e323be2ae537?w=900&q=85&auto=format&fit=crop" alt="BMW M8 Competition">
      </div>
      <div class="car-info">
        <div class="car-brand">BMW</div>
        <div class="car-name">M8 Competition</div>
        <div class="car-specs">
          <div class="car-spec"><div class="car-spec-val">4.4L</div><div class="car-spec-key">Engine</div></div>
          <div class="car-spec"><div class="car-spec-val">617 hp</div><div class="car-spec-key">Power</div></div>
          <div class="car-spec"><div class="car-spec-val">3.2s</div><div class="car-spec-key">0–100</div></div>
        </div>
        <div class="car-price-row">
          <div class="car-price">BD 38,500 <span>starting</span></div>
          <button class="car-btn" onclick="document.getElementById('booking').scrollIntoView({behavior:'smooth'})">Inquire</button>
        </div>
      </div>
    </div>

    <!-- Porsche -->
    <div class="car-card reveal" style="transition-delay:.3s">
      <div class="car-card-badge" style="background:transparent;border:1px solid #c0a060;color:#c0a060">Limited</div>
      <div class="car-image-wrap">
        <img class="car-photo" src="https://images.unsplash.com/photo-1503376780353-7e6692767b70?w=900&q=85&auto=format&fit=crop" alt="Porsche 911 Turbo S">
      </div>
      <div class="car-info">
        <div class="car-brand">Porsche</div>
        <div class="car-name">911 Turbo S</div>
        <div class="car-specs">
          <div class="car-spec"><div class="car-spec-val">3.8L</div><div class="car-spec-key">Engine</div></div>
          <div class="car-spec"><div class="car-spec-val">650 hp</div><div class="car-spec-key">Power</div></div>
          <div class="car-spec"><div class="car-spec-val">2.7s</div><div class="car-spec-key">0–100</div></div>
        </div>
        <div class="car-price-row">
          <div class="car-price">BD 68,000 <span>starting</span></div>
          <button class="car-btn" onclick="document.getElementById('booking').scrollIntoView({behavior:'smooth'})">Inquire</button>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- SERVICES -->
<section id="services">
  <div class="services-layout">
    <div class="reveal-left">
      <div class="section-label">What We Offer</div>
      <h2 class="section-title">Full-Spectrum <em>Automotive</em> Services</h2>
      <div class="gold-line"></div>
      <p class="section-subtitle">From acquisition to ongoing care, we offer a complete suite of luxury automotive services tailored to Bahrain's discerning clientele.</p>
    </div>
    <div class="services-grid reveal-right">
      <div class="service-card">
        <div class="service-icon">🚗</div>
        <div class="service-name">Buy a Car</div>
        <p class="service-desc">Browse our exclusive inventory of pre-vetted luxury vehicles. Every car certified and showroom-ready.</p>
      </div>
      <div class="service-card">
        <div class="service-icon">💰</div>
        <div class="service-name">Sell Your Car</div>
        <p class="service-desc">Get a premium valuation for your vehicle. Fair pricing, fast process, professional appraisal.</p>
      </div>
      <div class="service-card">
        <div class="service-icon">📅</div>
        <div class="service-name">Book Test Drive</div>
        <p class="service-desc">Experience your dream car before you buy. Personalized test drives at your convenience.</p>
      </div>
      <div class="service-card">
        <div class="service-icon">🏦</div>
        <div class="service-name">Finance Options</div>
        <p class="service-desc">Flexible financing solutions with competitive rates. Tailored packages for every budget.</p>
      </div>
      <div class="service-card">
        <div class="service-icon">🛡️</div>
        <div class="service-name">Warranty Plans</div>
        <p class="service-desc">Drive with confidence. Comprehensive warranty packages covering parts and labour.</p>
      </div>
      <div class="service-card">
        <div class="service-icon">✈️</div>
        <div class="service-name">Import & Export</div>
        <p class="service-desc">Can't find your dream car locally? We source and import from global markets seamlessly.</p>
      </div>
    </div>
  </div>
</section>

<!-- WHY US -->
<section id="why">
  <div class="why-layout">
    <div class="why-visual reveal-left">
      <div class="why-ring"></div>
      <div class="why-ring"></div>
      <div class="why-ring"></div>
      <div class="why-center">
        <div class="why-center-num">15+</div>
        <div class="why-center-label">Years</div>
      </div>
    </div>
    <div class="reveal-right">
      <div class="section-label">Why Choose Us</div>
      <h2 class="section-title">The Standard of <em>Luxury</em> in Bahrain</h2>
      <div class="gold-line"></div>
      <p class="section-subtitle">New Car Showroom isn't just a dealership — it's an experience. Every detail is crafted around you.</p>
      <div class="why-points">
        <div class="why-point">
          <div class="why-point-num">01</div>
          <div>
            <div class="why-point-title">Curated Luxury Collection</div>
            <p class="why-point-text">Only the finest vehicles make it to our showroom. Each car is personally inspected and certified to our exacting standards before we present it to you.</p>
          </div>
        </div>
        <div class="why-point">
          <div class="why-point-num">02</div>
          <div>
            <div class="why-point-title">White Glove Concierge Service</div>
            <p class="why-point-text">From first inquiry to key handover, our team provides personalised attention. We come to you, work around your schedule, and handle every detail.</p>
          </div>
        </div>
        <div class="why-point">
          <div class="why-point-num">03</div>
          <div>
            <div class="why-point-title">Transparent, Fair Pricing</div>
            <p class="why-point-text">No hidden fees, no pressure tactics. Our pricing is transparent, our valuations are fair, and our reputation is built on trust across 15 years in Bahrain.</p>
          </div>
        </div>
        <div class="why-point">
          <div class="why-point-num">04</div>
          <div>
            <div class="why-point-title">After-Sale Care & Support</div>
            <p class="why-point-text">Our relationship doesn't end at the sale. We provide ongoing maintenance referrals, warranty support, and trade-up programmes for valued clients.</p>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- BOOKING / CONTACT -->
<section id="booking">
  <div class="booking-layout">
    <div class="reveal-left">
      <div class="section-label">Get in Touch</div>
      <h2 class="section-title">Start Your <em>Journey</em> Today</h2>
      <div class="gold-line"></div>
      <p class="section-subtitle">Reserve a visit to our showroom, book a test drive, or request a private consultation with our luxury specialists.</p>

      <div class="booking-info-list">
        <div class="booking-info-item">
          <div class="booking-info-icon">📍</div>
          <div class="booking-info-text">
            <strong>Showroom Address</strong>
            <span>AI Burhama, Bahrain</span>
          </div>
        </div>
        <div class="booking-info-item">
          <div class="booking-info-icon">📞</div>
          <div class="booking-info-text">
            <strong>Phone & WhatsApp</strong>
            <span>+973 3331 9999</span>
          </div>
        </div>
        <div class="booking-info-item">
          <div class="booking-info-icon">✉️</div>
          <div class="booking-info-text">
            <strong>Email</strong>
            <span>info@newcarshowroom.bh</span>
          </div>
        </div>
        <div class="booking-info-item">
          <div class="booking-info-icon">🕐</div>
          <div class="booking-info-text">
            <strong>Opening Hours</strong>
            <span>Saturday: Opens 4:00 AM</span>
          </div>
        </div>
      </div>
    </div>

    <div class="reveal-right">
      <div class="booking-form">
        <div class="form-row">
          <div class="form-group">
            <label>First Name</label>
            <input type="text" placeholder="Ahmad">
          </div>
          <div class="form-group">
            <label>Last Name</label>
            <input type="text" placeholder="Al-Mansoori">
          </div>
        </div>
        <div class="form-row">
          <div class="form-group">
            <label>Phone Number</label>
            <input type="tel" placeholder="+973 ____">
          </div>
          <div class="form-group">
            <label>Email Address</label>
            <input type="email" placeholder="you@email.com">
          </div>
        </div>
        <div class="form-group">
          <label>Service Requested</label>
          <select>
            <option value="">Select a service…</option>
            <option>Buy a Vehicle</option>
            <option>Book Test Drive</option>
            <option>Sell My Car</option>
            <option>Finance Inquiry</option>
            <option>Warranty Plan</option>
            <option>Import / Export</option>
            <option>General Inquiry</option>
          </select>
        </div>
        <div class="form-group">
          <label>Vehicle of Interest</label>
          <input type="text" placeholder="e.g. Porsche 911, Mercedes G-Class…">
        </div>
        <div class="form-group">
          <label>Message</label>
          <textarea placeholder="Tell us how we can assist you…"></textarea>
        </div>
        <button class="form-submit" onclick="handleSubmit(event)">Send Inquiry →</button>
        <p id="form-msg" style="font-size:.72rem;color:var(--gold);letter-spacing:.1em;margin-top:.5rem;display:none;">✓ Thank you. Our team will reach out within 24 hours.</p>
      </div>
    </div>
  </div>
</section>

<!-- FOOTER -->
<footer>
  <div class="footer-top">
    <div>
      <div class="footer-brand-name">NEW CAR SHOWROOM</div>
      <div class="footer-brand-tag">Luxury Automotive · Bahrain</div>
      <p class="footer-brand-desc">Bahrain's premier destination for luxury and exotic automobiles. Serving discerning clients since 2009 with integrity, passion, and unparalleled expertise.</p>
      <div class="footer-socials">
        <a href="#" class="footer-social">in</a>
        <a href="#" class="footer-social">f</a>
        <a href="#" class="footer-social">ig</a>
        <a href="#" class="footer-social">yt</a>
      </div>
    </div>
    <div>
      <div class="footer-col-title">Quick Links</div>
      <ul class="footer-links">
        <li><a href="#inventory">Our Inventory</a></li>
        <li><a href="#services">Services</a></li>
        <li><a href="#why">About Us</a></li>
        <li><a href="#booking">Contact</a></li>
        <li><a href="#">News & Events</a></li>
      </ul>
    </div>
    <div>
      <div class="footer-col-title">Services</div>
      <ul class="footer-links">
        <li><a href="#">Buy a Car</a></li>
        <li><a href="#">Sell Your Car</a></li>
        <li><a href="#">Test Drive</a></li>
        <li><a href="#">Finance</a></li>
        <li><a href="#">Import / Export</a></li>
      </ul>
    </div>
    <div>
      <div class="footer-col-title">Contact</div>
      <ul class="footer-links">
        <li><a href="#">AI Burhama, Bahrain</a></li>
        <li><a href="tel:+97333319999">+973 3331 9999</a></li>
        <li><a href="mailto:info@newcarshowroom.bh">info@newcarshowroom.bh</a></li>
        <li><a href="#">Saturday: Opens 4:00 AM</a></li>
      </ul>
    </div>
  </div>
  <div class="footer-bottom">
    <span>© 2024 New Car Showroom. All rights reserved.</span>
    <span><a href="#">Privacy Policy</a> &nbsp;·&nbsp; <a href="#">Terms of Use</a> &nbsp;·&nbsp; Designed with precision in Bahrain</span>
  </div>
</footer>

<script>
// ── CURSOR ──
const cursor = document.getElementById('cursor');
const dot = document.getElementById('cursor-dot');
const ring = document.getElementById('cursor-ring');
let mx = 0, my = 0, rx = 0, ry = 0;
document.addEventListener('mousemove', e => { mx = e.clientX; my = e.clientY; dot.style.left = mx + 'px'; dot.style.top = my + 'px'; });
(function animRing() {
  rx += (mx - rx) * .14; ry += (my - ry) * .14;
  ring.style.left = rx + 'px'; ring.style.top = ry + 'px';
  requestAnimationFrame(animRing);
})();
document.querySelectorAll('a, button, .car-card, .service-card').forEach(el => {
  el.addEventListener('mouseenter', () => { ring.style.width = '56px'; ring.style.height = '56px'; ring.style.borderColor = 'var(--gold)'; });
  el.addEventListener('mouseleave', () => { ring.style.width = '36px'; ring.style.height = '36px'; ring.style.borderColor = 'var(--gold-light)'; });
});

// ── NAVBAR SCROLL ──
window.addEventListener('scroll', () => {
  document.getElementById('navbar').classList.toggle('scrolled', window.scrollY > 60);
});

// ── MOBILE MENU ──
document.getElementById('hamburger').addEventListener('click', () => document.getElementById('mobileMenu').classList.add('open'));
document.getElementById('menuClose').addEventListener('click', () => document.getElementById('mobileMenu').classList.remove('open'));
document.querySelectorAll('#mobileMenu a').forEach(a => a.addEventListener('click', () => document.getElementById('mobileMenu').classList.remove('open')));

// ── SCROLL REVEAL ──
const revealEls = document.querySelectorAll('.reveal, .reveal-left, .reveal-right');
const io = new IntersectionObserver(entries => {
  entries.forEach(e => { if (e.isIntersecting) { e.target.classList.add('visible'); io.unobserve(e.target); } });
}, { threshold: 0.12 });
revealEls.forEach(el => io.observe(el));

// ── COUNT UP ──
const counters = document.querySelectorAll('.stat-number[data-target]');
const cio = new IntersectionObserver(entries => {
  entries.forEach(e => {
    if (!e.isIntersecting) return;
    const el = e.target, target = +el.dataset.target, dur = 1800;
    let start = null;
    const step = ts => {
      if (!start) start = ts;
      const p = Math.min((ts - start) / dur, 1);
      el.textContent = Math.floor(p * target) + (target === 98 ? '%' : target === 15 ? '+' : '+');
      if (p < 1) requestAnimationFrame(step);
      else el.textContent = target + (target === 98 ? '%' : '+');
    };
    requestAnimationFrame(step);
    cio.unobserve(el);
  });
}, { threshold: .5 });
counters.forEach(c => cio.observe(c));

// ── FORM ──
function handleSubmit(e) {
  e.preventDefault();
  const msg = document.getElementById('form-msg');
  msg.style.display = 'block';
  setTimeout(() => msg.style.display = 'none', 5000);
}
</script>
</body>
</html>
